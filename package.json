{
  "name": "web3rsvp-subgraph2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ vaasanthk/web3rsvp-subgraph2",
    "create-local": "graph create --node http://localhost:8020/ vaasanthk/web3rsvp-subgraph2",
    "remove-local": "graph remove --node http://localhost:8020/ vaasanthk/web3rsvp-subgraph2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vaasanthk/web3rsvp-subgraph2",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
